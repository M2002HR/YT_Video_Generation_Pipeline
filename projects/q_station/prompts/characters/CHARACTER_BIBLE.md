# Deprecated Character Bible Compatibility Note

This path is retained so historical tooling can still open it. It is not an active universal-protagonist prompt. New Q Station runtime resolves identity through `characters/registry.json` and the selected character pack via `CharacterContext`. See `CHARACTER_REFERENCE_RULES.md`.

# Canonical red door

The first real run generates `red_door_design_sheet.png` with the existing shared entry-identity stage and a verified provider/content receipt. No manual image or placeholder is needed. The object sheet is topic-neutral; the later scene contains the actual topic world behind the hinged opening. Only the Newton character sheet is operator-installed for this integration.
